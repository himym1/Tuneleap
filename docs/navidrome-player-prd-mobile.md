# 功能文档：Navidrome Player（移动端）

**版本**：1.0  
**日期**：2026-02-28  
**平台**：Android（当前代码主验证）/ iOS（移动端形态设计覆盖）  
**设计稿**：`docs/designs/pencil/navidrome/navidrome-mobile.pen`

---

## 1. 文档目标

本文件定义 Navidrome Player 在移动端的功能范围、实现状态与后续规划，覆盖：

1. 当前已实现能力（代码现状）。
2. 已有 UI 设计覆盖（Pencil `.pen`）。
3. 已实现能力、部分实现能力与后续规划（Phase 2/3/4）。

---

## 2. 移动端产品目标

1. 让用户在手机上快速完成连接、找歌、播放闭环。
2. 通过底部导航与全屏播放页提升单手操作效率。
3. 保证后台播放、锁屏控制和通知栏控制的稳定性。
4. 在网络不稳定时逐步支持离线播放能力。

---

## 3. 信息架构（移动端）

### 3.1 一级导航

- 首页（Home）
- 音乐库（Library）
- 搜索（Search）
- 播放列表（Playlists）
- 设置（Settings）

### 3.2 全局区域

- 底部 MiniPlayer（持久显示）
- 全屏 Player（由 MiniPlayer 展开）
- 下载入口（Phase 4 强化）

---

## 4. 功能清单（移动端）

状态定义：

- 已实现：代码已可用。
- 部分实现：已有主链路，但稳定性、交互细节或覆盖范围仍待补强。
- 未实现：仅规划或仅有 API 能力。

| 模块 | 代码状态 | 设计稿状态 | 说明 |
|---|---|---|---|
| 账号登录与服务器连接 | 已实现 | 已覆盖 | 登录流程可用 |
| 首页发现（最近添加） | 已实现 | 已覆盖 | 首页基础内容已存在 |
| 音乐库（艺术家/专辑） | 已实现 | 已覆盖 | 已有艺术家/专辑/歌曲三栏浏览 |
| 搜索（search3） | 已实现 | 已覆盖 | 已接搜索、防抖与结果筛选 |
| 播放核心控制（播/停/切歌/循环/随机） | 已实现 | 已覆盖 | 含随机、循环、进度、倍速、音量基础能力 |
| 后台播放与系统媒体控制 | 部分实现 | 部分覆盖 | 已接入 `audio_service`，仍需补强稳定性与平台细节 |
| 全屏播放页 | 已实现 | 已覆盖 | 已有封面、进度、歌词、队列、倍速、音量等主链路 |
| 播放队列管理（查看/重排/删除） | 部分实现 | 部分覆盖 | 已有查看/重排/删除，清空等高级交互待补 |
| 播放列表管理 | 部分实现 | 已覆盖 | 已支持列表读取、创建、删除 |
| 收藏与同步 | 部分实现 | 未覆盖 | 已有收藏页与操作入口，仍需统一状态与稳定性补强 |
| 设置中心（服务器/主题/缓存/关于） | 部分实现 | 部分覆盖 | 已有主题、音质、缓存信息、关于、退出等设置 |
| 离线下载与离线播放 | 部分实现 | 已覆盖 | 已支持单曲下载与本地文件回退播放 |
| 音质选择 | 已实现 | 未覆盖 | 音质档位选择 |
| Scrobble 播放记录 | 部分实现 | 未覆盖 | 已有自动上报，缺状态反馈与失败重试 |
| 多服务器支持 | 部分实现 | 未覆盖 | 已有服务器列表、增删改与切换 |
| 歌词系统 | 部分实现 | 未覆盖 | 已有歌词展示与同步高亮基础 |
| 动态主题与动效增强 | 部分实现 | 部分覆盖 | 已有播放器封面取色与部分转场，尚未全局化 |

---

## 5. 页面清单（移动端设计稿）

当前 `navidrome-mobile.pen` 页面：

1. `01 Login`
2. `02 Home`
3. `03 Library`
4. `04 Search`
5. `05 Player`
6. `06 Playlists`
7. `07 Downloads`
8. `08 Settings`

建议补充的交互细节：

1. 下载任务状态（排队/暂停/失败重试）
2. 收藏状态统一来源与失败回滚
3. 播放队列的清空、批量操作与更多手势
4. 歌词视图（同步高亮已具备，点击跳播待补）

---

## 6. 与阶段规划对齐（移动端）

### 6.1 Phase 2（好用）

优先补强：后台播放稳定性、播放列表编辑能力、队列高级交互。

### 6.2 Phase 3（好看）

优先补强：播放器视觉升级、全局动态主题、歌词交互、动效细节。

### 6.3 Phase 4（完善）

优先补强：下载任务体系、Scrobble 重试、多服务器隔离策略。

---

## 7. 验收标准（移动端）

1. 首次连接 3 分钟内可完成并开始播放。
2. 切后台后可稳定播放，锁屏控制与应用状态 1 秒内一致。
3. 播放页拖拽进度误差小于 1 秒。
4. 下载后离线场景可播放已缓存内容。
5. 关键页面完整：首页、音乐库、搜索、播放器、播放列表、下载、设置。
